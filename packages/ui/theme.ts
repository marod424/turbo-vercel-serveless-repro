import { MantineThemeOverride } from "@mantine/core";

export const theme: MantineThemeOverride = {
  fontFamily: 'Nunito',
  headings: { fontFamily: 'Nunito', fontWeight: 500 },
  primaryColor: 'violet',
  spacing: { xs: 16, sm: 24, md: 32, lg: 40, xl: 64 }
}